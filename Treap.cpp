#include <bits/stdc++.h>

using namespace std;

#define all(v) (v).begin(), (v).end()
#define rep(i, a, b) for(int (i) = (a); (i) < (b); ++(i))


using ll = long long;
using vi = vector<int>;

struct node {
    int val, prio;

    node( int _v, int _p ) : val(_v), prio(_p) {}

};

struct treap {
    
};


int main()
{

}
